import create, { itemCreate } from './src/createStyle';
import { deviceWidth, deviceHeight, setBaseWidth, rpx } from './src/screenUtil';
import { unit2Num as unitFmt } from './src/styleTranslateMethods';

export { create, deviceWidth, deviceHeight, setBaseWidth, rpx, itemCreate, unitFmt };

export default {
  create,
  deviceWidth,
  deviceHeight,
  setBaseWidth,
  rpx,
  itemCreate,
  unitFmt,
};
